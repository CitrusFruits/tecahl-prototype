/*
 * Returns whether the given space, using indices on the global.map,
 * is occupied.
 * argument0: xIndex
 * argument1: yIndex
 */
 if(global.map[argument0, argument1] == 0){
    return false;
 }
 return true;
